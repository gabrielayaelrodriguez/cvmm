<div class="center-align">

<h1>Virtual Machines</h1>

</div>

<div class="row">
<%= search_form_for @search, url: user_virtual_machines_path, :class => "col s12" do |f| %>

  <div class="row">

    <div class="input-field col s4">
      <%= f.label :name_cont %>
      <%= f.search_field :name_cont %>
    </div>
    <div class="input-field col s4">
      <%= f.submit "Search", :class => 'grey waves-effect waves-light btn' %>
    </div>

  </div>
<% end %>

</div>

<table>
  <thead>
    <tr>
      <th><%= sort_link(@search, :name, 'Name', default_order: :desc) %></th>
      <th><%= sort_link(@search, :state, 'State', default_order: :desc) %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @virtual_machines.each do |virtual_machine| %>
      <tr>
        <td><%= virtual_machine.name %></td>
        <td><%= virtual_machine.state %></td>
        <td><%= link_to 'Show', user_virtual_machine_path(virtual_machine) %></td>
        <td><%= link_to 'Edit', edit_user_virtual_machine_path(virtual_machine) %></td>
        <td><%= link_to 'Destroy', user_virtual_machine_path(virtual_machine), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<br>

<div class="center-align">

<%= paginate @virtual_machines %>

<br>

<%= link_to 'New Virtual Machine', new_user_virtual_machine_path, :class => 'waves-effect waves-light btn' %>

</div>
<br><br>

<%= link_to '« Back', admin_dashboard_index_path %>

<br><br>