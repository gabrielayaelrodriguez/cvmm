<%= form_with(model: users_virtual_machine, local: true) do |form| %>
  <% if users_virtual_machine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(users_virtual_machine.errors.count, "error") %> prohibited this users_virtual_machine from being saved:</h2>

      <ul>
      <% users_virtual_machine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :state %>
    <%= form.text_field :state, id: :users_virtual_machine_state %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
