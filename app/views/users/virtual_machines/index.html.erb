<p id="notice"><%= notice %></p>

<h1>Users Virtual Machines</h1>
<% if @users_virtual_machines.any? %>

<table>
  <thead>
    <tr>
      <th>State</th>
      <th colspan="3"></th>
    </tr>
  </thead>

 

  <tbody>
    <% @users_virtual_machines.each do |users_virtual_machine| %>
      <tr>
        <td><%= users_virtual_machine.state %></td>
        <td><%= link_to 'Show', users_virtual_machine %></td>
        <td><%= link_to 'Edit', edit_users_virtual_machine_path(users_virtual_machine) %></td>
        <td><%= link_to 'Destroy', users_virtual_machine, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<% else %>

  nada

<% end %>
<%= link_to 'New Users Virtual Machine', new_users_virtual_machine_path %>
